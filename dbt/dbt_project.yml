name: "fx_macro"
version: "0.1.0"
config-version: 2

profile: "fx_macro"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets: ["target", "dbt_packages", "logs"]

models:
  fx_macro:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    mart:
      +materialized: table
